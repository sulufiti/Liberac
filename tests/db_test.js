// const Knex = require('knex')
// const knexConfig = require('../knexfile')
// const knex = Knex(knexConfig[process.env.NODE_ENV || 'development'])

// knex('users')
// .then((users) => {
//   for (user of users) {
//     console.log(`${user.first_name} ${user.last_name} lives at ${user.address}`)
//   }
// })
// .then(() => {
//   process.exit(0)
// })
// .catch((err) => {
//   console.log(err)
// })